<?php
$user_id = $dados['user_id'];
$query_pesquisa = $dados['query_pesquisa'];
$resultados = $dados['resultados'];
$mensagem_erro = $dados['mensagem_erro'];
$seguidorModel = $dados['seguidorModel'];
?>
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clone X</title>
    <link rel="stylesheet" href="../public/style/style.css"> 
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    
    <style>
        .main-content-wrapper {
            margin-left: 58px;
            padding: 20px;
        }

        .search-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .search-container input[type="text"] {
            width: calc(100% - 70px);
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        .search-container button[type="submit"] {
            width: 60px;
            padding: 10px;
            background: #000;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 5px;
        }
        .resultado {
            display: flex;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #eee;
        }
        .resultado img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-right: 15px;
            object-fit: cover;
        }
        .resultado .info {
            flex-grow: 1;
        }
        .resultado .info h4 {
            margin: 0;
            font-size: 1.1em;
        }
        .resultado .info p {
            margin: 0;
            color: #657786;
        }
        .resultado form button, .resultado a.btn-seguir {
            padding: 8px 15px;
            font-size: 0.9em;
            font-weight: bold;
            border-radius: 20px;
            min-width: 120px;
            text-align: center;
            display: inline-block;
        }
        .btn-seguir {
            background-color: #000;
            color: #fff;
            border: none;
        }
        .btn-seguindo {
            background-color: #fff;
            color: #000;
            border: 1px solid #ccc;
        }
    </style>
</head>
<body>
<header>
    <nav id="navbar">
        <div id="navbar-inner">
            <ul>
                <li class="item-menu">
                    <a href="FeedController.php"><span class="icon"><i class="bi bi-house"></i></span></a>
                </li>
                <li class="item-menu ativo">
                    <a href="BuscaController.php"><span class="icon"><i class="bi bi-search"></i></span></a>
                </li>
                <li class="item-menu">
                    <a href="#"><span class="icon"><i class="bi bi-plus-circle-fill"></i></span></a>
                </li>
                <li class="item-menu">
                    <a href="PerfilController.php"><span class="icon"><i class="bi bi-person-circle"></i></span></a>
                </li>
                <li class="item-menu">
                    <a href="AuthController.php?action=logout"><span class="icon"><i class="bi bi-box-arrow-right" title="Logout"></i></span></a>
                </li>
            </ul>
        </div>
    </nav>
</header>

<div class="main-content-wrapper">
    <div class="search-container">
        <h2>Pesquisar Usuários</h2>
        <form method="GET" action="BuscaController.php" style="display: flex;">
            <input type="text" name="q" placeholder="Buscar por nome ou @username" value="<?php echo htmlspecialchars($query_pesquisa); ?>" required>
            <button type="submit"><i class="bi bi-search"></i></button>
        </form>
    </div>

    <div class="search-container" style="margin-top: 20px;">
        <?php if (!empty($mensagem_erro)): ?>
            <div class="message-box error-message" style="margin-bottom: 15px;">
                <?php echo $mensagem_erro; ?>
            </div>
        <?php endif; ?>

        <?php if (!empty($query_pesquisa)): ?>
            <?php if (empty($resultados)): ?>
                <p style="text-align: center;">Nenhum usuário encontrado para "<?php echo htmlspecialchars($query_pesquisa); ?>".</p>
            <?php else: ?>
                <?php foreach ($resultados as $usuario_encontrado): 
                    $status_seguindo = $seguidorModel->isFollowing($user_id, $usuario_encontrado['id']);
                    $foto_usuario = !empty($usuario_encontrado['foto']) ? 'data:image/jpeg;base64,' . base64_encode($usuario_encontrado['foto']) : '../public/img/padrao.png';
                ?>
                    <div class="resultado">
                        <img src="<?php echo htmlspecialchars($foto_usuario); ?>" alt="Foto de perfil">
                        <div class="info">
                            <h4><?php echo htmlspecialchars($usuario_encontrado['nome']); ?></h4>
                            <p>@<?php echo htmlspecialchars($usuario_encontrado['username']); ?></p>
                        </div>
                        
                        <?php if ($status_seguindo !== 'self'): ?>
                            <form method="POST" action="BuscaController.php?q=<?php echo urlencode($query_pesquisa); ?>">
                                <input type="hidden" name="usuario_id" value="<?php echo $usuario_encontrado['id']; ?>">
                                <?php if ($status_seguindo): ?>
                                    <input type="hidden" name="acao" value="deixar_de_seguir">
                                    <button type="submit" class="btn-seguindo">Seguindo</button>
                                <?php else: ?>
                                    <input type="hidden" name="acao" value="seguir">
                                    <button type="submit" class="btn-seguir">Seguir</button>
                                <?php endif; ?>
                            </form>
                        <?php else: ?>
                            <a href="PerfilController.php" class="btn-seguir" style="text-decoration: none; text-align: center;">Meu Perfil</a>
                        <?php endif; ?>

                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        <?php endif; ?>
    </div>
</div>
</body>
</html>